forzar las notificaciones:

http://localhost/prestamos_app/api/notificaciones_auto.php?action=run

forzar el crear la numeración de los prestamos:

SELECT 
    cp.id_cliente,
    COUNT(*) AS total_prestamos_cliente
FROM cliente_prestamo cp
GROUP BY cp.id_cliente;
